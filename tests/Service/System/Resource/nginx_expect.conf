# Fusio (https://www.fusio-project.org/)
# Generated for Nginx on 0000-00-00
# api.apioo.de
server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html/fusio;
    server_name api.apioo.de;

    location / {
        try_files $uri $uri/ =404;
    }

    error_log /tmp/error.log warn;
    access_log /tmp/access.log combined;

}

server {
    listen 443 ssl;
    listen [::]:443 ssl;

    root /var/www/html/fusio;
    server_name api.apioo.de;

    location / {
        try_files $uri $uri/ =404;
    }

    error_log /tmp/error.log warn;
    access_log /tmp/access.log combined;


    ssl_certificate /tmp/domain.crt;
    ssl_certificate_key /tmp/private.key;
}

# fusio.apioo.de
server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html/fusio/fusio;
    server_name fusio.apioo.de;

    location / {
        try_files $uri $uri/ =404;
    }


}


# documentation.apioo.de
server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html/fusio/documentation;
    server_name documentation.apioo.de;

    location / {
        try_files $uri $uri/ =404;
    }


}


# developer.apioo.de
server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html/fusio/developer;
    server_name developer.apioo.de;

    location / {
        try_files $uri $uri/ =404;
    }


}

